import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
  notificationIcon: {
    marginRight: '20px',
  },
  notificationsMenu: {
    marginTop: '10px',
  },
}))

export default useStyles
